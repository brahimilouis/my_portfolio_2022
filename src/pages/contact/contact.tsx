import React from 'react';
import './_contact.scss'
export type ContactProps = {}

export default function Contact({}: ContactProps) {
    return (<section className="contact">
        <h1 className={"titre-section"}>Me contacter</h1>
    </section>);
}
